{
  "name": "@trubittech/nft-deploy-functions",
  "scripts": {
    "postinstall": "cd functions && yarn",
    "lint": "eslint --ext .js,.ts .",
    "build": "tsc",
    "format": "prettier --config .prettierrc 'functions/src/**/*.ts' --write",
    "build:watch": "tsc --watch",
    "serve": "yarn run build && yarn firebase emulators:start --only functions",
    "shell": "yarn run build && yarn firebase functions:shell",
    "start": "yarn run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "dev": "yarn run build && MODE=debug FIRESTORE_EMULATOR_HOST=localhost:8080 yarn firebase emulators:start --config=\"./firebase.json\""
  },
  "engines": {
    "node": "16"
  },
  "devDependencies": {
    "@types/node": "^18.7.13",
    "@types/node-fetch": "^2.6.2",
    "@typescript-eslint/eslint-plugin": "^5.12.0",
    "@typescript-eslint/parser": "^5.12.0",
    "eslint": "^8.9.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.25.4",
    "eslint-plugin-prettier": "^4.2.1",
    "prettier": "^2.7.1",
    "firebase-functions-test": "^0.2.0",
    "typescript": "^4.5.4"
  },
  "private": true
}
