import { styled } from '../stitches.config'

type WalletActivityFeedProps = {}

export const WalletActivityFeed = styled('div', {
  /*
   * # https://stitches.dev/docs/styling#base-styles
   * # backgroundColor: '$primary',
   * # color: '$hiContrast'
   */
})
