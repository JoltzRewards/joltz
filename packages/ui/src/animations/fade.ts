import { keyframes } from '../stitches.config'

export const _in = keyframes({
  '0%': { opacity: 0 },
  '100%': { opacity: 1 },
})

export const _out = keyframes({
  '0%': { opacity: 1 },
  '100%': { opacity: 0 },
})
